// const step = (deltaTime: number) => {
//   // TODO: 把dt传递给各个模块

import { generateBrickRow } from "./modules/map/utils/BrickGroupFactory";

// };
console.log(generateBrickRow(5))
export default {
  // step,
};
